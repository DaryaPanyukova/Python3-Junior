st=input("Введите предложение ")
sim=" "
nom=0
summa=""
for a in range(len(st)):
    
    if st[a]==sim:

        slovo=st[nom:a]

        nom=nom+len(slovo)+1
        summa=summa+slovo
slovo2=st[nom:len(st)]
print(summa+slovo2)
